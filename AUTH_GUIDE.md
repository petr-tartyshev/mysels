# 🔐 Руководство по авторизации SELS

## ✅ Что реализовано:

### 1. **Страницы авторизации**
- ✅ `/login` - Страница входа
- ✅ `/register` - Страница регистрации
- ✅ Главная страница с кнопками "Войти" и "Зарегистрироваться"

### 2. **API Endpoints**
- ✅ `POST /api/auth/login` - Вход в систему
- ✅ `POST /api/auth/register` - Регистрация нового пользователя
- ✅ `POST /api/auth/logout` - Выход из системы
- ✅ `GET /api/auth/me` - Получить текущего пользователя

### 3. **Система сессий**
- ✅ Cookies для хранения сессии (userId, username)
- ✅ Автоматическая проверка авторизации
- ✅ Защита профиля (требует авторизации)

### 4. **Тестовые аккаунты**

#### **Petr Tartyshev**
- Email: `petr@sels.com`
- Username: `petrtar`
- Пароль: `password123`

#### **Сергей Иванов**
- Email: `sergey@sels.com`
- Username: `sergeyiv`
- Пароль: `password123`

---

## 🚀 Как использовать:

### **1. Вход через главную страницу:**

```
http://localhost:3000
```

Нажмите кнопку **"Войти"** → откроется страница логина

---

### **2. Быстрый вход (тестовые аккаунты):**

На странице `/login` есть кнопки для быстрого входа:
- **Petr Tartyshev** - один клик для входа
- **Сергей Иванов** - один клик для входа

---

### **3. Регистрация нового пользователя:**

```
http://localhost:3000/register
```

Заполните форму:
- Имя
- Фамилия
- Username (уникальный)
- Email (уникальный)
- Пароль (минимум 6 символов)
- Подтверждение пароля

---

### **4. Просмотр профилей:**

#### **Свой профиль:**
```
http://localhost:3000/profile
```

#### **Профиль другого пользователя:**
```
http://localhost:3000/profile?username=sergeyiv
```

---

### **5. Выход из системы:**

В своём профиле нажмите кнопку **"Выйти"** (иконка LogOut) в правом верхнем углу.

---

## 📋 Функциональность:

### **На своём профиле:**
- ✅ Видна кнопка создания поста (+)
- ✅ Видна кнопка настроек
- ✅ Видна кнопка выхода
- ✅ Можно создавать посты

### **На чужом профиле:**
- ❌ Кнопка создания поста скрыта
- ❌ Кнопка настроек скрыта
- ✅ Можно только просматривать посты

---

## 🔒 Безопасность:

- ✅ Пароли хешируются с помощью bcrypt
- ✅ Cookies с httpOnly для userId
- ✅ Валидация данных на сервере
- ✅ Проверка уникальности email и username

---

## 🎯 Примеры использования:

### **Сценарий 1: Вход как Petr**
1. Откройте http://localhost:3000
2. Нажмите "Войти"
3. Нажмите кнопку "Petr Tartyshev" (быстрый вход)
4. Вы попадёте на свой профиль

### **Сценарий 2: Просмотр профиля Сергея**
1. Войдите как Petr
2. Откройте http://localhost:3000/profile?username=sergeyiv
3. Вы увидите профиль Сергея (без кнопки создания поста)

### **Сценарий 3: Регистрация нового пользователя**
1. Откройте http://localhost:3000/register
2. Заполните форму
3. После регистрации автоматически войдёте в систему

---

## 🛠️ Технические детали:

### **Структура cookies:**
```
userId: <user_id> (httpOnly, 7 дней)
username: <username> (доступен в клиенте, 7 дней)
```

### **Формат ответа API:**

**Логин:**
```json
{
  "success": true,
  "user": {
    "id": "...",
    "email": "petr@sels.com",
    "username": "petrtar",
    "firstName": "Petr",
    "lastName": "Tartyshev"
  }
}
```

**Регистрация:**
```json
{
  "success": true,
  "user": {
    "id": "...",
    "email": "new@user.com",
    "username": "newuser",
    "firstName": "New",
    "lastName": "User"
  }
}
```

---

## 📝 Примечания:

- Пароли для тестовых аккаунтов: `password123`
- Сессия хранится в cookies 7 дней
- После выхода cookies удаляются
- Профиль автоматически определяет, свой это профиль или чужой

---

**🎉 Система авторизации полностью готова к использованию!**
