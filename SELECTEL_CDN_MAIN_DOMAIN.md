# 🚀 Настройка основного домена mysels.ru через Selectel CDN

## 🎯 Цель
Настроить `mysels.ru` так, чтобы он работал через Selectel CDN (быстрее), а CDN проксировал запросы к Vercel.

---

## 📋 План действий

1. **В Selectel CDN:** Настроить origin на Vercel
2. **В Selectel CDN:** Добавить домен `www.mysels.ru` (так как CNAME для корня не работает)
3. **В DNS REG.RU:** Настроить `www.mysels.ru` → Selectel CDN
4. **В DNS REG.RU:** Настроить `mysels.ru` → редирект на `www.mysels.ru` (или через A-запись)

---

## 📝 ШАГ 1: Настройка Origin в Selectel CDN

### 1.1. Открой настройки CDN-ресурса

1. Зайди в Selectel → **CDN** → **CDN-ресурсы**
2. Выбери свой ресурс **"selsCDN"**
3. Открой вкладку **"Общий"** (если не открыта)

### 1.2. Найди настройку Origin (Источник)

В настройках CDN-ресурса должна быть секция **"Origin"** или **"Источник"**.

**Что нужно указать:**

1. **Тип источника:** `HTTP` или `HTTPS`
2. **Адрес источника (Origin):** 
   - Введи адрес твоего Vercel проекта
   - Формат: `mysels-q5bqijiu2-petr-tartyshevs-projects.vercel.app`
   - Или если есть кастомный домен в Vercel: `mysels.vercel.app`
   
   **Где найти адрес Vercel:**
   - Vercel Dashboard → проект → **Settings** → **Domains**
   - Там будет указан дефолтный домен Vercel (что-то вроде `mysels-xxxxx.vercel.app`)

3. **Порт:** `443` (для HTTPS) или `80` (для HTTP)

4. **Host header (если есть настройка):**
   - Оставь пустым или укажи тот же адрес Vercel
   - Это нужно, чтобы Vercel понимал, какой домен запрашивается

### 1.3. Сохрани настройки Origin

Нажми кнопку **"Сохранить"** или **"Применить"**

---

## 📝 ШАГ 2: Добавь домен www.mysels.ru в Selectel CDN

### 2.1. Добавь персональный домен

1. В том же разделе **"Персональные домены"** нажми **"Добавить домен"**
2. В поле **"Домен"** введи: `www.mysels.ru`
3. **НЕ нажимай "Подключить" пока!** Сначала настроим DNS.

---

## 📝 ШАГ 3: Настройка DNS в REG.RU

### 3.1. Удали старые записи (если есть)

1. REG.RU → **Мои домены** → `mysels.ru` → **DNS-серверы и зона**
2. Удали все CNAME записи для `mysels.ru` (если есть)

### 3.2. Добавь CNAME для www.mysels.ru

1. Нажми **"+ Добавить запись"**
2. Тип: **CNAME**
3. **Subdomain:** `www`
4. **Canonical name:** `0a512cf1-5486-44ea-9f2d-c8b998527b13.selcdn.net.` (с точкой в конце)
5. Нажми **"Готово"**

### 3.3. Настрой редирект mysels.ru → www.mysels.ru

**Вариант А: Через A-запись (если Selectel дает IP)**

Если в Selectel есть возможность получить IP-адрес для CDN:

1. В REG.RU добавь **A-запись**:
   - **Subdomain:** оставь пустым (или `@`)
   - **Значение:** IP-адрес Selectel CDN
   - (IP нужно получить в поддержке Selectel или в настройках CDN)

**Вариант Б: Через редирект на уровне DNS (если поддерживается)**

Некоторые регистраторы поддерживают редирект на уровне DNS. Проверь в REG.RU, есть ли такая опция.

**Вариант В: Через Vercel (проще всего)**

1. В Vercel → проект → **Settings** → **Domains**
2. Добавь домен `mysels.ru` (если еще не добавлен)
3. В настройках домена выбери **"Redirect to Another Domain"**
4. Укажи: `www.mysels.ru`
5. Тип редиректа: **301 Moved Permanently**

---

## 📝 ШАГ 4: Вернись в Selectel и подключи домен

1. Вернись в Selectel CDN → форма "Добавление домена"
2. Убедись, что введено: `www.mysels.ru`
3. Нажми **"Подключить"**

Selectel проверит DNS и подключит домен.

---

## ⏱️ ШАГ 5: Ожидание и проверка

1. **Подожди 10-30 минут** (обновление DNS)
2. **Проверь работу:**
   - Открой `https://www.mysels.ru` - должен работать через Selectel CDN
   - Открой `https://mysels.ru` - должен редиректить на `www.mysels.ru`

---

## ✅ Итоговый результат

- **`www.mysels.ru`** → Selectel CDN → Vercel (быстро!)
- **`mysels.ru`** → редирект 301 → `www.mysels.ru` (тоже быстро!)

**Пользователи будут автоматически перенаправляться на `www.mysels.ru`, который работает через CDN.**

---

## 🔧 Альтернатива: Если нужен именно mysels.ru без www

Если обязательно нужен `mysels.ru` без `www`:

1. **Свяжись с поддержкой Selectel** и спроси:
   - Можно ли использовать A-запись для корневого домена?
   - Какой IP-адрес использовать для CDN?

2. Или используй **Selectel Load Balancer** вместо CDN:
   - Load Balancer может работать с A-записями
   - Но это более сложная настройка

---

## 🆘 Если что-то не работает

### Selectel не видит домен:
- Убедись, что DNS-запись добавлена правильно
- Проверь через https://dnschecker.org
- Подожди еще 15-20 минут

### Сайт не открывается:
- Проверь, что Origin настроен правильно в Selectel
- Убедись, что Vercel домен доступен
- Проверь логи в Selectel CDN

---

**Готово!** Теперь основной сайт будет грузиться быстрее через Selectel CDN 🚀
