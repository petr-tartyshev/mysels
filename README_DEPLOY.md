# Автоматический деплой в GitHub

## ✅ Настройка завершена!

Токен GitHub сохранен в файле `.github_token` (не коммитится в git).

## 🚀 Использование

### 1. Отправить изменения в GitHub:
```bash
./push-to-github.sh
```

Скрипт автоматически:
- ✅ Проверит наличие изменений
- ✅ Добавит все изменения (кроме `.github/workflows/`)
- ✅ Создаст коммит
- ✅ Отправит в GitHub (без запроса пароля)

**Примечание:** Изменения в `.github/workflows/` пропускаются, так как токен не имеет права `workflow`. См. `UPDATE_TOKEN.md` для обновления токена.

### 2. Полный деплой (GitHub + сервер):
```bash
./deploy.sh
```

Скрипт автоматически:
- ✅ Отправит изменения в GitHub
- ✅ Обновит код на сервере
- ✅ Пересоберет Docker образ
- ✅ Применит миграции БД
- ✅ Перезапустит веб-сервер

## ⚠️ Важно

### Токен не имеет права `workflow`

Текущий токен не может изменять файлы в `.github/workflows/`. 

**Для изменения workflow файлов:**
1. Создай новый токен с правами `workflow` (см. `UPDATE_TOKEN.md`)
2. Или используй SSH для push workflow файлов вручную

**Для обычных изменений кода:**
- ✅ Скрипт работает автоматически
- ✅ Все изменения отправляются без проблем

## 🔒 Безопасность

- Токен хранится в `.github_token` (в `.gitignore`)
- Файл имеет права доступа 600 (только владелец)
- Токен не попадет в git репозиторий

## 📝 Обновление токена

Если нужно обновить токен:
1. Отредактируй файл `.github_token`
2. Вставь новый токен
3. Сохрани файл

Подробнее: `UPDATE_TOKEN.md`

## ✅ Проверка

Проверить, что все работает:
```bash
./push-to-github.sh
```

Если нет изменений, скрипт сообщит об этом.
