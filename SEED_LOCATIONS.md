# Добавление всех локаций в базу данных

Этот скрипт добавляет все локации с карты в базу данных Supabase:
- Уличные площадки (5 локаций)
- Веломаршруты (3 маршрута)
- Водные маршруты для сапов/байдарок (4 маршрута)

## Запуск на сервере

### Вариант 1: Через Docker контейнер

```bash
ssh root@45.130.8.3
cd /root/sels
git pull origin main
docker compose exec web npm run seed:locations
```

### Вариант 2: Локально (если есть доступ к DATABASE_URL)

```bash
cd /Users/petr/sels
npm run seed:locations
```

## Что делает скрипт

1. Проверяет, существует ли локация с таким именем
2. Если не существует - создаёт новую
3. Если существует - пропускает (не дублирует)

## Результат

После выполнения вы увидите:
- ✅ Сколько локаций создано
- ⏭️ Сколько пропущено (уже существовали)
- 📍 Общее количество локаций

## Типы локаций

- `outdoor` - Уличные площадки
- `bike` - Веломаршруты
- `water` - Водные маршруты (сапы/байдарки)

Все локации будут доступны для выбора при создании постов и событий.
