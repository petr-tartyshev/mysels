# 🔍 Проверка подключения к базе данных Supabase

Быстрая проверка, что всё настроено для регистрации пользователей.

---

## ✅ Чеклист

### 1. DATABASE_URL в Vercel

**Проверь:**
1. Vercel Dashboard → проект → **Settings** → **Environment Variables**
2. Должна быть переменная:
   - **Key:** `DATABASE_URL`
   - **Value:** строка подключения к Supabase
   - **Environment:** Production, Preview, Development (все три)

**Если нет:**
1. Зайди в **Supabase Dashboard** → твой проект
2. **Settings** → **Database** → **Connection string** → **URI**
3. Скопируй строку (формат: `postgresql://postgres:[PASSWORD]@db.xxxxx.supabase.co:5432/postgres`)
4. В Vercel → **Add** → Key: `DATABASE_URL`, Value: (вставь строку)
5. Выбери все окружения (Production, Preview, Development)
6. **Save**
7. **Redeploy** проект (Deployments → последний деплой → ⋯ → Redeploy)

---

### 2. Миграции применены к Supabase

**Проверь:**
```bash
# Установи DATABASE_URL
export DATABASE_URL="postgresql://postgres:[PASSWORD]@db.xxxxx.supabase.co:5432/postgres"

# Проверь статус миграций
npx prisma migrate status
```

**Если миграции не применены:**
```bash
# Примени миграции
npx prisma migrate deploy
```

**Должно вывести:**
```
✅ All migrations have been successfully applied.
```

---

### 3. Таблицы созданы в Supabase

**Проверь:**
1. Supabase Dashboard → твой проект → **Table Editor**
2. Должны быть таблицы:
   - `users`
   - `posts`
   - `events`
   - `locations`
   - `conversations`
   - `messages`
   - и другие

**Если таблиц нет:**
- Примени миграции (см. шаг 2)

---

### 4. Проверка подключения

**Тест через Prisma Studio (опционально):**
```bash
export DATABASE_URL="postgresql://postgres:[PASSWORD]@db.xxxxx.supabase.co:5432/postgres"
npx prisma studio
```

Должен открыться Prisma Studio с таблицами.

---

## 🐛 Если регистрация не работает

### Ошибка "Ошибка соединения с сервером"

**Причина:** Проблема с подключением к БД

**Решение:**
1. Проверь `DATABASE_URL` в Vercel (см. шаг 1)
2. Проверь логи Vercel:
   - Deployments → последний деплой → **Functions Logs**
   - Ищи ошибки типа "Can't reach database server" или "Connection refused"
3. Проверь, что Supabase проект активен:
   - Supabase Dashboard → проект должен быть в статусе "Active"
4. Проверь настройки сети Supabase:
   - Settings → **Database** → **Connection pooling**
   - Убедись, что подключение разрешено

### Ошибка "Prisma Client не инициализирован"

**Причина:** Prisma Client не сгенерирован

**Решение:**
1. В Vercel должен быть `postinstall` скрипт: `"postinstall": "prisma generate"`
2. Проверь, что он выполняется при деплое
3. Если нет, добавь в `package.json`:
   ```json
   {
     "scripts": {
       "postinstall": "prisma generate"
     }
   }
   ```

---

## 📝 После проверки

Если всё настроено правильно:

1. Открой `https://www.mysels.ru/register`
2. Заполни форму регистрации
3. Нажми "Зарегистрироваться"
4. Должен произойти автоматический вход

**Если всё равно ошибка:**
- Пришли текст ошибки из консоли браузера (F12 → Console)
- Или логи из Vercel Functions Logs

---

**Готово!** После проверки всех пунктов регистрация должна работать 🚀
