# 🚀 Развертывание SELS на mysels.ru

Пошаговая инструкция для размещения проекта на домене `mysels.ru` с сохранением всех мультиплеер-функций.

---

## 📋 Что будет сделано

1. ✅ Настройка production базы данных (общая для всех пользователей)
2. ✅ Деплой на Vercel (Next.js хостинг)
3. ✅ Подключение домена `mysels.ru`
4. ✅ Настройка DNS
5. ✅ Применение миграций БД
6. ✅ Создание staging окружения

---

## 🗄️ Шаг 1: Создание Production базы данных

**Важно:** База данных будет общей для всех пользователей (мультиплеер).

### Вариант 1: Supabase (рекомендую - бесплатный тариф)

1. Зайдите на https://supabase.com
2. Нажмите "Start your project" → войдите через GitHub
3. Создайте новый проект:
   - **Название:** `mysels-production`
   - **Database Password:** придумайте сложный пароль (сохраните!)
   - **Region:** `Europe West` (ближайший к России)
4. Дождитесь создания (2-3 минуты)
5. После создания:
   - Откройте "Settings" → "Database"
   - Найдите "Connection string" → "URI"
   - Скопируйте строку подключения (она выглядит так):
     ```
     postgresql://postgres:[YOUR-PASSWORD]@db.xxxxx.supabase.co:5432/postgres
     ```
   - **Сохраните эту строку!** Это ваш `DATABASE_URL` для прода

### Вариант 2: Neon (бесплатный PostgreSQL)

1. Зайдите на https://neon.tech
2. Зарегистрируйтесь через GitHub
3. Создайте проект:
   - **Name:** `mysels-production`
   - **Region:** выберите ближайший
4. После создания скопируйте `Connection String` из дашборда

### Вариант 3: Railway (простой, но платный после пробного периода)

1. Зайдите на https://railway.app
2. Войдите через GitHub
3. "New Project" → "Database" → "PostgreSQL"
4. Скопируйте `DATABASE_URL` из настроек

---

## 🌐 Шаг 2: Деплой на Vercel

### 2.1. Регистрация на Vercel

1. Зайдите на https://vercel.com
2. Нажмите "Sign Up"
3. Войдите через GitHub (используйте тот же аккаунт, где репозиторий `mysels`)

### 2.2. Подключение репозитория

1. В Vercel Dashboard нажмите "Add New..." → "Project"
2. Выберите репозиторий `petr-tartyshev/mysels`
3. Настройки проекта:
   - **Framework Preset:** Next.js (определится автоматически)
   - **Root Directory:** `./` (по умолчанию)
   - **Build Command:** `npm run build` (по умолчанию)
   - **Output Directory:** `.next` (по умолчанию)
   - **Install Command:** `npm install` (по умолчанию)
4. **НЕ нажимайте "Deploy" пока!** Сначала настроим переменные окружения

### 2.3. Настройка переменных окружения

1. В форме деплоя найдите "Environment Variables"
2. Добавьте следующие переменные:

   ```
   DATABASE_URL = [ваша строка подключения из Supabase/Neon/Railway]
   NODE_ENV = production
   NEXT_PUBLIC_ENV = production
   ```

3. **Важно:** Убедитесь, что `DATABASE_URL` указан для всех окружений:
   - Production
   - Preview
   - Development

4. Теперь нажмите "Deploy"

5. Дождитесь завершения деплоя (2-5 минут)

---

## 🗄️ Шаг 3: Применение миграций к Production БД

После создания БД нужно создать таблицы в базе данных.

### Через Vercel CLI (рекомендую):

1. Установите Vercel CLI (если еще не установлен):
   ```bash
   npm i -g vercel
   ```

2. Войдите в Vercel:
   ```bash
   vercel login
   ```

3. В корне проекта выполните:
   ```bash
   # Установите DATABASE_URL из прода (замените на вашу строку)
   export DATABASE_URL="postgresql://postgres:[PASSWORD]@db.xxxxx.supabase.co:5432/postgres"
   
   # Примените миграции
   npx prisma migrate deploy
   
   # Заполните тестовыми данными (опционально, можно пропустить)
   npx prisma db seed
   ```

### Альтернатива: через Supabase Dashboard

1. Зайдите в Supabase → ваш проект → "SQL Editor"
2. Откройте файл `prisma/migrations/20260121212220_init/migration.sql`
3. Скопируйте содержимое и выполните в SQL Editor
4. Повторите для всех миграций в папке `prisma/migrations/`

---

## 🌍 Шаг 4: Подключение домена mysels.ru

### 4.1. В Vercel:

1. Зайдите в проект → "Settings" → "Domains"
2. Введите домен: `mysels.ru`
3. Нажмите "Add"
4. Vercel покажет DNS-записи, которые нужно добавить

**Обычно Vercel просит добавить:**
- Тип: `A` или `CNAME`
- Имя: `@` (для mysels.ru) или `www` (для www.mysels.ru)
- Значение: IP-адрес или домен Vercel (например, `cname.vercel-dns.com`)

### 4.2. В панели управления доменом:

**Где настраивать DNS зависит от регистратора:**

#### Если домен на REG.RU:
1. Зайдите в личный кабинет REG.RU
2. Перейдите в "Мои домены" → выберите `mysels.ru`
3. Откройте "DNS-серверы и зона"
4. Убедитесь, что используется "DNS REG.RU"
5. Добавьте записи, которые показал Vercel:
   - Для `mysels.ru` (без www):
     - Тип: `A` или `CNAME`
     - Имя: `@`
     - Значение: адрес из Vercel
   - Для `www.mysels.ru`:
     - Тип: `CNAME`
     - Имя: `www`
     - Значение: адрес из Vercel
6. Сохраните изменения

#### Если домен на Timeweb:
1. Личный кабинет → "Домены" → `mysels.ru`
2. "Управление DNS"
3. Добавьте записи аналогично

#### Если домен на другом регистраторе:
- Найдите раздел "DNS", "DNS-зона", "Управление DNS"
- Добавьте записи, которые показал Vercel

### 4.3. Ожидание обновления DNS:

- **Обычно:** 1-2 часа
- **Максимум:** 24 часа
- **Проверка:** можно проверить через https://dnschecker.org

После обновления DNS сайт будет доступен по адресу `https://mysels.ru`

---

## 🧪 Шаг 5: Создание Staging окружения

### 5.1. Создание Staging БД:

1. В Supabase/Neon/Railway создайте второй проект:
   - **Название:** `mysels-staging`
   - Скопируйте `DATABASE_URL` для стейджа

### 5.2. Создание Staging проекта в Vercel:

1. В Vercel создайте новый проект из того же репозитория
2. **Название:** `mysels-staging`
3. В "Environment Variables" добавьте:
   ```
   DATABASE_URL = [строка подключения к стейдж-БД]
   NODE_ENV = production
   NEXT_PUBLIC_ENV = staging
   ```
4. Задеплойте проект

### 5.3. Применение миграций к Staging БД:

```bash
export DATABASE_URL="[строка подключения к стейдж-БД]"
npx prisma migrate deploy
npx prisma db seed
```

### 5.4. Подключение Staging домена (опционально):

1. В Vercel → проект `mysels-staging` → "Settings" → "Domains"
2. Добавьте домен: `stage.mysels.ru` (или `staging.mysels.ru`)
3. Настройте DNS аналогично шагу 4.2:
   - Тип: `CNAME`
   - Имя: `stage` (или `staging`)
   - Значение: адрес из Vercel

**Или оставьте на дефолтном домене Vercel** (например, `mysels-staging.vercel.app`) и не публикуйте ссылку.

---

## ✅ Чеклист готовности

- [ ] Production БД создана (Supabase/Neon/Railway)
- [ ] `DATABASE_URL` скопирован и сохранен
- [ ] Проект задеплоен на Vercel
- [ ] Переменные окружения настроены в Vercel
- [ ] Миграции применены к production БД
- [ ] Домен `mysels.ru` добавлен в Vercel
- [ ] DNS-записи настроены у регистратора
- [ ] Сайт открывается по `https://mysels.ru`
- [ ] Staging БД создана
- [ ] Staging проект создан в Vercel
- [ ] Миграции применены к staging БД

---

## 🔒 Важные моменты для мультиплеера

### ✅ Что будет работать:

1. **Общая база данных** - все пользователи видят одни и те же:
   - События (events)
   - Посты (posts)
   - Локации (locations)
   - Сообщения в чатах (messages)
   - Уведомления (notifications)

2. **Регистрация и авторизация** - пользователи могут:
   - Регистрироваться
   - Входить в систему
   - Создавать посты и события
   - Общаться в чатах

3. **Карта** - все пользователи видят:
   - Одни и те же точки на карте
   - События других пользователей
   - Локации

### ⚠️ Что нужно проверить после деплоя:

1. **Регистрация нового пользователя** - работает ли?
2. **Создание поста** - сохраняется ли в БД?
3. **Создание события** - видно ли другим пользователям?
4. **Чат** - отправляются ли сообщения?
5. **Карта** - отображаются ли все точки?

---

## 🐛 Решение проблем

### Сайт не открывается по домену:

1. Проверьте DNS-записи (может быть еще не обновились):
   ```bash
   # Проверка через терминал
   nslookup mysels.ru
   # или
   dig mysels.ru
   ```

2. Проверьте, что домен подключен в Vercel:
   - Проект → "Settings" → "Domains" → должен быть статус "Valid"

3. Проверьте SSL-сертификат:
   - Vercel автоматически выдает SSL, но может занять время

### Ошибка подключения к БД:

1. Проверьте `DATABASE_URL` в Vercel:
   - "Settings" → "Environment Variables"
   - Убедитесь, что строка правильная

2. Проверьте, что БД запущена:
   - В Supabase/Neon/Railway проверьте статус проекта

3. Проверьте, что миграции применены:
   ```bash
   export DATABASE_URL="[ваш DATABASE_URL]"
   npx prisma migrate status
   ```

### Ошибки при деплое:

1. Проверьте логи в Vercel:
   - "Deployments" → выберите деплой → "Build Logs"

2. Убедитесь, что все зависимости установлены:
   - Проверьте `package.json`

3. Проверьте, что `prisma generate` выполняется:
   - В `package.json` есть `"postinstall": "prisma generate"`

---

## 📊 Мониторинг после деплоя

### Что проверить:

1. **Vercel Dashboard:**
   - "Analytics" - количество запросов
   - "Functions" - логи API routes

2. **База данных:**
   - Supabase/Neon Dashboard - использование ресурсов
   - Количество записей в таблицах

3. **Производительность:**
   - Скорость загрузки страниц
   - Время ответа API

---

## 💰 Стоимость

**Бесплатный вариант (для старта):**
- Домен `mysels.ru`: уже куплен ✅
- Vercel: бесплатно (до 100GB bandwidth/месяц)
- Supabase: бесплатно (до 500MB БД, 2GB bandwidth)
- **Итого: 0₽/месяц**

**Платный вариант (при росте):**
- Vercel Pro: $20/месяц (~2000₽/месяц)
- Supabase Pro: $25/месяц (~2500₽/месяц)
- **Итого: ~4500₽/месяц**

---

## 🎯 Итоговая структура

```
Production:
- Домен: mysels.ru
- Хостинг: Vercel (mysels-production проект)
- БД: Supabase/Neon (mysels-production)
- URL: https://mysels.ru

Staging:
- Домен: stage.mysels.ru (или mysels-staging.vercel.app)
- Хостинг: Vercel (mysels-staging проект)
- БД: Supabase/Neon (mysels-staging)
- URL: https://stage.mysels.ru
```

---

## 📞 Следующие шаги

После успешного деплоя:

1. Протестируйте все функции:
   - Регистрация/вход
   - Создание постов
   - Создание событий
   - Чат
   - Карта

2. Настройте мониторинг (опционально):
   - Sentry для отслеживания ошибок
   - Vercel Analytics для аналитики

3. Настройте резервное копирование БД:
   - Supabase делает автоматически
   - Для других провайдеров настройте вручную

---

**Готово!** Теперь ваш проект доступен на `https://mysels.ru` с полной поддержкой мультиплеера! 🚀
