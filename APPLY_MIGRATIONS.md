# 🗄️ Применение миграций к Supabase

Инструкция по применению миграций к production базе данных.

---

## 🚀 Быстрый способ

### Шаг 1: Получи DATABASE_URL из Supabase

1. Зайди в **Supabase Dashboard** → твой проект
2. Открой **Settings** → **Database**
3. Найди раздел **"Connection string"** → **"URI"**
4. Скопируй строку подключения (формат: `postgresql://postgres:[PASSWORD]@db.xxxxx.supabase.co:5432/postgres`)

### Шаг 2: Примени миграции

Выполни в терминале:

```bash
# Установи DATABASE_URL (замени на свою строку из Supabase)
export DATABASE_URL="postgresql://postgres:[PASSWORD]@db.xxxxx.supabase.co:5432/postgres"

# Перейди в корень проекта
cd /Users/petr/sels

# Примени миграции
npx prisma migrate deploy
```

**Должно вывести:**
```
✅ All migrations have been successfully applied.
```

---

## 📋 Что будет создано

После применения миграций будут созданы таблицы:

- ✅ `users` - пользователи
- ✅ `posts` - посты
- ✅ `events` - события
- ✅ `locations` - локации
- ✅ `conversations` - беседы
- ✅ `conversation_participants` - участники бесед
- ✅ `messages` - сообщения
- ✅ `notifications` - уведомления
- ✅ `bookings` - бронирования
- ✅ `event_requests` - запросы на участие в событиях

---

## ✅ Проверка

### 1. Проверь статус миграций

```bash
export DATABASE_URL="postgresql://postgres:[PASSWORD]@db.xxxxx.supabase.co:5432/postgres"
npx prisma migrate status
```

**Должно показать:**
```
✅ Database schema is up to date!
```

### 2. Проверь таблицы в Supabase

1. Зайди в **Supabase Dashboard** → твой проект
2. Открой **Table Editor**
3. Должны быть все таблицы из списка выше

### 3. Проверь через Prisma Studio (опционально)

```bash
export DATABASE_URL="postgresql://postgres:[PASSWORD]@db.xxxxx.supabase.co:5432/postgres"
npx prisma studio
```

Должен открыться Prisma Studio с таблицами.

---

## 🐛 Если что-то не работает

### Ошибка "Can't reach database server"

**Причина:** Неправильный DATABASE_URL или БД недоступна

**Решение:**
1. Проверь, что DATABASE_URL правильный (скопирован из Supabase)
2. Проверь, что Supabase проект активен
3. Проверь, что пароль в строке подключения правильный

### Ошибка "Migration already applied"

**Причина:** Миграции уже применены

**Решение:**
- Это нормально, значит всё уже настроено
- Проверь таблицы в Supabase Dashboard

### Ошибка "Table does not exist"

**Причина:** Миграции не применены

**Решение:**
- Примени миграции: `npx prisma migrate deploy`

---

## 📝 Альтернативный способ: через Supabase SQL Editor

Если CLI не работает, можно применить миграции через Supabase:

1. Зайди в **Supabase Dashboard** → твой проект
2. Открой **SQL Editor**
3. Открой файл `prisma/migrations/20260121212220_init/migration.sql`
4. Скопируй содержимое
5. Вставь в SQL Editor и выполни
6. Повтори для всех миграций по порядку

---

**Готово!** После применения миграций можно регистрировать пользователей 🚀
