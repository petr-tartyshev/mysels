# Archived sections

The following user-facing sections were archived and removed from active navigation:

- `Map` (`/map`)
- `News` (`/news`)

Current behavior:

- Links to these sections were removed from the main navigation.
- Direct visits to `/map` or `/news` now redirect to `/profile`.

Restore options:

- Use git history for `app/map/page.tsx` and `app/news/page.tsx` to recover previous implementations.
