# 🔧 Пересборка контейнера БЕЗ кеша

## Проблема

Docker использует кеш, поэтому изменения в Dockerfile не применяются.

## Решение

Пересобрать контейнер БЕЗ кеша.

## На сервере выполни:

```bash
cd /root/sels
docker compose build --no-cache
docker compose up -d
docker logs sels_web --tail 20
```

После пересборки OpenSSL должен быть установлен и Prisma должен работать.

---

**Выполни команды на сервере и попробуй зарегистрироваться снова!**
