# 🚀 Быстрый старт - База данных SELS

## 📦 Шаг 1: Установка зависимостей

```bash
npm install
```

Это установит:
- ✅ `@prisma/client` - Prisma Client для работы с БД
- ✅ `prisma` - Prisma CLI
- ✅ `bcryptjs` - Хеширование паролей
- ✅ `ts-node` - Для запуска seed

---

## 🐘 Шаг 2: Запуск PostgreSQL

### Вариант A: Docker (Рекомендуется)

```bash
# Запустить PostgreSQL в контейнере
docker-compose up -d

# Проверить, что контейнер запущен
docker ps
```

### Вариант B: Homebrew (macOS)

```bash
# Запустить PostgreSQL
brew services start postgresql@15

# Создать базу данных
createdb sels_db
```

### Вариант C: Cloud Database

1. Создайте бесплатную БД на [Supabase](https://supabase.com) или [Railway](https://railway.app)
2. Скопируйте connection string

---

## ⚙️ Шаг 3: Настройка .env

Создайте файл `.env` в корне проекта:

```bash
touch .env
```

Добавьте следующее содержимое:

```env
# Database (выберите один из вариантов)

# Для Docker:
DATABASE_URL="postgresql://sels_user:sels_password@localhost:5432/sels_db?schema=public"

# Для Homebrew:
# DATABASE_URL="postgresql://username@localhost:5432/sels_db?schema=public"

# Для Cloud (Supabase/Railway):
# DATABASE_URL="postgresql://user:password@host:port/database"

# NextAuth (сгенерируйте секрет)
NEXTAUTH_URL="http://localhost:3000"
NEXTAUTH_SECRET="YOUR_SECRET_HERE"

# Socket.io (для чата в будущем)
NEXT_PUBLIC_SOCKET_URL="http://localhost:3001"
```

### Генерация секрета:

```bash
openssl rand -base64 32
```

Скопируйте результат в `NEXTAUTH_SECRET`

---

## 🗄️ Шаг 4: Применение миграций

```bash
# Применить миграции (создать таблицы)
npx prisma migrate dev --name init
```

Это создаст:
- ✅ Таблица `users` (пользователи)
- ✅ Таблица `posts` (публикации)
- ✅ Таблица `locations` (локации)
- ✅ Таблица `events` (события)
- ✅ Таблица `bookings` (бронирования)
- ✅ Таблица `conversations` (беседы)
- ✅ Таблица `messages` (сообщения)
- ✅ Таблица `notifications` (уведомления)
- ✅ Prisma Client (автоматически)

---

## 🌱 Шаг 5: Заполнение начальными данными

```bash
npx prisma db seed
```

Это создаст:
- ✅ 2 тестовых пользователя (petr@sels.com, sergey@sels.com)
- ✅ 5 локаций (Яуза, Сокольники, Лужники, Парк Горького, Воробьевы горы)
- ✅ 2 события на 21.01.2026
- ✅ 2 поста
- ✅ 1 беседу с сообщениями
- ✅ 2 уведомления

**Тестовый логин:**
- Email: `petr@sels.com`
- Password: `password123`

---

## 🎨 Шаг 6: Просмотр данных (опционально)

```bash
# Открыть Prisma Studio
npx prisma studio
```

Откроется браузер с GUI для просмотра и редактирования данных.

---

## 🚀 Шаг 7: Запуск приложения

```bash
npm run dev
```

Откройте http://localhost:3000

---

## ✅ Проверка работы

### 1. API Endpoints работают:

```bash
# Получить все локации
curl http://localhost:3000/api/locations

# Получить все посты
curl http://localhost:3000/api/posts

# Получить пользователя по username
curl http://localhost:3000/api/users?username=petrtar
```

### 2. Prisma Studio открывается:

```bash
npx prisma studio
```

### 3. Данные в БД:

```bash
# Подключиться к PostgreSQL (Docker)
docker exec -it sels_postgres psql -U sels_user -d sels_db

# Проверить таблицы
\dt

# Посмотреть пользователей
SELECT * FROM users;

# Выйти
\q
```

---

## 🔧 Полезные команды

```bash
# Пересоздать БД (ОСТОРОЖНО! Удалит все данные)
npx prisma migrate reset

# Регенерировать Prisma Client
npx prisma generate

# Форматировать schema.prisma
npx prisma format

# Проверить схему на ошибки
npx prisma validate

# Открыть Prisma Studio
npx prisma studio
```

---

## 📁 Структура файлов

```
/Users/petr/sels/
├── prisma/
│   ├── schema.prisma      # Схема базы данных ✅
│   └── seed.ts            # Начальные данные ✅
├── lib/
│   └── prisma.ts          # Prisma Client ✅
├── app/
│   └── api/
│       ├── posts/
│       │   └── route.ts   # API для постов ✅
│       ├── users/
│       │   └── route.ts   # API для пользователей ✅
│       ├── conversations/
│       │   └── route.ts   # API для чатов ✅
│       ├── messages/
│       │   └── route.ts   # API для сообщений ✅
│       ├── notifications/
│       │   └── route.ts   # API для уведомлений ✅
│       └── locations/
│           └── route.ts   # API для локаций ✅
├── .env                   # Настройки окружения (создайте!)
├── docker-compose.yml     # Docker для PostgreSQL ✅
└── DATABASE_SETUP.md      # Подробная документация ✅
```

---

## ❓ Что-то пошло не так?

### Ошибка: "Can't reach database server"

**Решение:**
```bash
# Проверьте, запущен ли PostgreSQL
docker ps

# Или
brew services list
```

### Ошибка: "P1001: Can't reach database server"

**Решение:**
- Проверьте `DATABASE_URL` в `.env`
- Убедитесь, что PostgreSQL запущен

### Ошибка: "Environment variable not found: DATABASE_URL"

**Решение:**
- Создайте файл `.env` в корне проекта
- Добавьте `DATABASE_URL`

### Ошибка: "Invalid prisma.xxx invocation"

**Решение:**
```bash
npx prisma generate
```

---

## 📚 Дальнейшие шаги

1. ✅ **Интеграция с /profile** - Загружать посты из БД
2. ✅ **Интеграция с /map** - Загружать локации и события из БД
3. ✅ **Интеграция с /chats** - Реализовать чат с БД
4. ✅ **Аутентификация** - NextAuth.js с БД
5. ✅ **Notifications** - Система уведомлений

---

**🎉 База данных готова к использованию!**

Подробная документация: `DATABASE_SETUP.md`
