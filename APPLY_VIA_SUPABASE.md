# 🚀 Применение миграций через Supabase SQL Editor

Самый простой способ - выполнить SQL напрямую в Supabase.

---

## 📝 Шаг 1: Открой SQL Editor

1. Зайди в **Supabase Dashboard** → твой проект
2. В левом меню найди **"SQL Editor"**
3. Нажми **"New query"** (или кнопку создания нового запроса)

---

## 📝 Шаг 2: Скопируй и выполни SQL

1. Открой файл `all-migrations.sql` в проекте
2. Скопируй **всё содержимое** файла (Ctrl+A, Ctrl+C)
3. Вставь в SQL Editor в Supabase (Ctrl+V)
4. Нажми кнопку **"Run"** (или Ctrl+Enter)

**Должно вывести:**
```
Success. No rows returned
```

---

## ✅ Шаг 3: Проверь результат

1. В Supabase Dashboard открой **Table Editor**
2. Обнови страницу (F5)
3. Должны появиться таблицы:
   - ✅ `users`
   - ✅ `posts`
   - ✅ `events`
   - ✅ `locations`
   - ✅ `conversations`
   - ✅ `conversation_participants`
   - ✅ `messages`
   - ✅ `notifications`
   - ✅ `bookings`
   - ✅ `event_requests`

---

## 🎉 Готово!

После создания таблиц:

1. **Попробуй зарегистрировать пользователя:**
   - Открой `https://www.mysels.ru/register`
   - Заполни форму
   - Должна пройти регистрация

2. **Проверь в Supabase:**
   - Table Editor → таблица `users`
   - Должна появиться запись с новым пользователем

---

## 🆘 Если ошибка

### Ошибка "relation already exists"

**Причина:** Таблица уже существует

**Решение:**
- Это нормально, значит таблица уже создана
- Продолжай проверку других таблиц

### Ошибка "syntax error"

**Причина:** Неправильно скопирован SQL

**Решение:**
- Убедись, что скопировал весь файл `all-migrations.sql`
- Проверь, что нет лишних символов

---

**Готово!** После выполнения SQL все таблицы будут созданы 🚀
