# 🔧 Исправление ошибки Prisma SSL

## Проблема

Prisma не может загрузить библиотеку `libssl.so.1.1` в Alpine Linux контейнере.

## Решение

Установить OpenSSL в Dockerfile. Исправление уже применено.

## На сервере выполни:

```bash
cd /root/sels
docker compose build
docker compose up -d
docker logs sels_web --tail 20
```

После пересборки Prisma должен работать корректно.

---

**Выполни команды на сервере и попробуй зарегистрироваться снова!**
