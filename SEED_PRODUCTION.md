# 🌱 Применение seed в продакшене

Инструкция по созданию тестовых пользователей в production базе данных.

---

## 📋 Созданные пользователи

После применения seed будут созданы **3 тестовых пользователя**:

1. **Petr Tartyshev**
   - Email: `petr@sels.com`
   - Пароль: `password123`
   - Username: `petrtar`

2. **Сергей Иванов**
   - Email: `sergey@sels.com`
   - Пароль: `password123`
   - Username: `sergeyiv`

3. **Анна Петрова**
   - Email: `anna@sels.com`
   - Пароль: `password123`
   - Username: `annapet`

**Также создается технический аккаунт SELS:**
- Email: `sels@system.com` (для системных уведомлений)

---

## 🚀 Способ 1: Через Vercel CLI (рекомендуется)

### Шаг 1: Установи Vercel CLI (если еще не установлен)

```bash
npm i -g vercel
```

### Шаг 2: Войди в Vercel

```bash
vercel login
```

### Шаг 3: Подключись к production базе данных

```bash
# Установи DATABASE_URL из продакшена
export DATABASE_URL="postgresql://postgres:[PASSWORD]@[HOST]:5432/postgres"
```

**Где взять DATABASE_URL:**
- Если используешь **Supabase**: Settings → Database → Connection string
- Если используешь **Neon**: Dashboard → Connection string
- Если используешь **Railway**: Database → Connect → Connection string

### Шаг 4: Примени seed

```bash
# Убедись, что находишься в корне проекта
cd /Users/petr/sels

# Примени seed
npx prisma db seed
```

### Шаг 5: Проверь результат

Должно вывести:
```
✅ Пользователи созданы: { user1: 'petrtar', user2: 'sergeyiv', user3: 'annapet', ... }
📋 Данные для входа:
1. petr@sels.com / password123
2. sergey@sels.com / password123
3. anna@sels.com / password123
```

---

## 🚀 Способ 2: Через Supabase Dashboard (если используешь Supabase)

### Шаг 1: Открой SQL Editor

1. Зайди в Supabase Dashboard → твой проект
2. Открой **SQL Editor**

### Шаг 2: Выполни SQL-скрипт

Скопируй и выполни следующий SQL (замени `[HASHED_PASSWORD]` на хеш пароля):

```sql
-- Создание пользователей
INSERT INTO users (id, email, password, "firstName", "lastName", username, avatar, bio, "createdAt", "updatedAt")
VALUES 
  (
    gen_random_uuid()::text,
    'petr@sels.com',
    '$2a$10$rBV2jW2J5K5K5K5K5K5K5O5K5K5K5K5K5K5K5K5K5K5K5K5K5K5K5K5K', -- password123
    'Petr',
    'Tartyshev',
    'petrtar',
    '/avatars/petr.jpg',
    'Основатель SELS. Люблю футбол и активный образ жизни!',
    NOW(),
    NOW()
  ),
  (
    gen_random_uuid()::text,
    'sergey@sels.com',
    '$2a$10$rBV2jW2J5K5K5K5K5K5K5O5K5K5K5K5K5K5K5K5K5K5K5K5K5K5K5K5K5K', -- password123
    'Сергей',
    'Иванов',
    'sergeyiv',
    '/avatars/sergey.jpg',
    'Тренер по футболу. Организую тренировки для всех уровней.',
    NOW(),
    NOW()
  ),
  (
    gen_random_uuid()::text,
    'anna@sels.com',
    '$2a$10$rBV2jW2J5K5K5K5K5K5K5O5K5K5K5K5K5K5K5K5K5K5K5K5K5K5K5K5K5K', -- password123
    'Анна',
    'Петрова',
    'annapet',
    '/avatars/anna.jpg',
    'Люблю бег и йогу. Организую утренние пробежки в парках Москвы.',
    NOW(),
    NOW()
  )
ON CONFLICT (email) DO NOTHING;
```

**⚠️ Проблема:** Нужно знать хеш пароля. Лучше использовать **Способ 1** (через CLI).

---

## 🚀 Способ 3: Через локальный скрипт

### Шаг 1: Создай временный скрипт

Создай файл `seed-production.ts` в корне проекта:

```typescript
import { PrismaClient } from '@prisma/client'
import bcrypt from 'bcryptjs'

const prisma = new PrismaClient({
  datasources: {
    db: {
      url: process.env.DATABASE_URL, // Из production
    },
  },
})

async function main() {
  const hashedPassword = await bcrypt.hash('password123', 10)

  const users = [
    {
      email: 'petr@sels.com',
      password: hashedPassword,
      firstName: 'Petr',
      lastName: 'Tartyshev',
      username: 'petrtar',
      avatar: '/avatars/petr.jpg',
      bio: 'Основатель SELS. Люблю футбол и активный образ жизни!',
    },
    {
      email: 'sergey@sels.com',
      password: hashedPassword,
      firstName: 'Сергей',
      lastName: 'Иванов',
      username: 'sergeyiv',
      avatar: '/avatars/sergey.jpg',
      bio: 'Тренер по футболу. Организую тренировки для всех уровней.',
    },
    {
      email: 'anna@sels.com',
      password: hashedPassword,
      firstName: 'Анна',
      lastName: 'Петрова',
      username: 'annapet',
      avatar: '/avatars/anna.jpg',
      bio: 'Люблю бег и йогу. Организую утренние пробежки в парках Москвы.',
    },
  ]

  for (const userData of users) {
    const user = await prisma.user.upsert({
      where: { email: userData.email },
      update: {},
      create: userData,
    })
    console.log(`✅ Создан пользователь: ${user.email}`)
  }

  console.log('🎉 Все пользователи созданы!')
}

main()
  .catch(console.error)
  .finally(() => prisma.$disconnect())
```

### Шаг 2: Запусти скрипт

```bash
# Установи DATABASE_URL из продакшена
export DATABASE_URL="postgresql://postgres:[PASSWORD]@[HOST]:5432/postgres"

# Запусти скрипт
npx ts-node seed-production.ts
```

---

## ✅ Проверка

После применения seed:

1. Открой `https://www.mysels.ru/login`
2. Попробуй войти с любым из созданных пользователей:
   - `petr@sels.com` / `password123`
   - `sergey@sels.com` / `password123`
   - `anna@sels.com` / `password123`

3. Если вход успешен, значит всё работает! 🎉

---

## 🔒 Безопасность

**⚠️ Важно:** После создания пользователей в продакшене:

1. **Смени пароли** на более сложные (через форму регистрации или вручную)
2. **Не используй** `password123` в продакшене для реальных пользователей
3. Эти аккаунты **только для тестирования**

---

## 🆘 Если что-то не работает

### Ошибка подключения к БД:
- Проверь `DATABASE_URL` - правильный ли?
- Убедись, что БД доступна из интернета
- Проверь, что IP твоего компьютера разрешен в настройках БД

### Ошибка "User already exists":
- Это нормально, значит пользователь уже создан
- Seed использует `upsert`, так что не создаст дубликаты

### Ошибка при выполнении seed:
- Проверь, что все миграции применены: `npx prisma migrate deploy`
- Проверь логи ошибки

---

**Готово!** После применения seed у тебя будут 3 тестовых пользователя для проверки всего функционала 🚀
