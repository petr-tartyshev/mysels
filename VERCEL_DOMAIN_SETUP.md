# 🌐 Подключение домена mysels.ru к Vercel

Пошаговая инструкция по подключению домена `mysels.ru` к вашему проекту в Vercel.

---

## 📝 Шаг 1: Добавление домена в Vercel

1. Зайдите в **Vercel Dashboard** → выберите ваш проект
2. Откройте вкладку **"Settings"** (в верхнем меню)
3. В левом меню выберите **"Domains"**
4. В поле "Domain" введите: `mysels.ru`
5. Нажмите кнопку **"Add"** или **"Add Domain"**

---

## 📝 Шаг 2: Получение DNS-записей от Vercel

После добавления домена Vercel покажет DNS-записи, которые нужно добавить:

**Обычно Vercel просит добавить:**

### Для основного домена (mysels.ru):
- **Тип:** `A` или `CNAME`
- **Имя:** `@` (или оставить пустым)
- **Значение:** IP-адрес или домен Vercel (например, `76.76.21.21` или `cname.vercel-dns.com`)

### Для поддомена www (www.mysels.ru):
- **Тип:** `CNAME`
- **Имя:** `www`
- **Значение:** домен Vercel (например, `cname.vercel-dns.com`)

**⚠️ Важно:** Скопируйте точные значения, которые покажет Vercel! Они могут отличаться для каждого проекта.

---

## 📝 Шаг 3: Настройка DNS у регистратора домена

Где настраивать DNS зависит от того, где вы купили домен:

### Если домен на REG.RU:

1. Зайдите в личный кабинет REG.RU
2. Перейдите в **"Мои домены"** → выберите `mysels.ru`
3. Откройте **"DNS-серверы и зона"**
4. Убедитесь, что используется **"DNS REG.RU"** (не сторонние DNS)
5. Нажмите **"Управление DNS-записями"**
6. Добавьте записи, которые показал Vercel:

   **Для mysels.ru:**
   - Нажмите **"Добавить запись"**
   - **Тип:** `A` (если Vercel дал IP) или `CNAME` (если дал домен)
   - **Имя:** `@` (или оставить пустым)
   - **Значение:** скопируйте из Vercel
   - **TTL:** `3600` (или по умолчанию)
   - Нажмите **"Сохранить"**

   **Для www.mysels.ru:**
   - Нажмите **"Добавить запись"**
   - **Тип:** `CNAME`
   - **Имя:** `www`
   - **Значение:** скопируйте из Vercel
   - **TTL:** `3600`
   - Нажмите **"Сохранить"**

### Если домен на Timeweb:

1. Личный кабинет → **"Домены"** → `mysels.ru`
2. Откройте **"Управление DNS"**
3. Добавьте записи аналогично REG.RU

### Если домен на другом регистраторе:

- Найдите раздел **"DNS"**, **"DNS-зона"**, **"Управление DNS"** или **"DNS Records"**
- Добавьте записи, которые показал Vercel

---

## 📝 Шаг 4: Ожидание обновления DNS

После добавления DNS-записей:

- **Обычно:** 1-2 часа
- **Максимум:** 24 часа
- **Минимум:** 5-10 минут (редко)

**Проверка обновления DNS:**
- Используйте https://dnschecker.org
- Введите `mysels.ru` и выберите тип записи `A` или `CNAME`
- Проверьте, что записи обновились по всему миру

---

## 📝 Шаг 5: Проверка в Vercel

После обновления DNS:

1. Вернитесь в Vercel → проект → **Settings** → **Domains**
2. Статус домена должен измениться на **"Valid"** (зеленая галочка)
3. Vercel автоматически выдаст SSL-сертификат (может занять несколько минут)

---

## ✅ Готово!

После обновления DNS ваш сайт будет доступен по адресу:
- **https://mysels.ru**
- **https://www.mysels.ru** (если добавили запись для www)

---

## 🐛 Решение проблем

### Домен не обновляется

1. **Проверьте DNS-записи:**
   - Убедитесь, что записи добавлены правильно
   - Проверьте, что значения скопированы точно из Vercel

2. **Проверьте DNS-серверы:**
   - Убедитесь, что используются DNS вашего регистратора
   - Не используйте сторонние DNS (Cloudflare, Google DNS) без настройки

3. **Очистите DNS-кэш:**
   ```bash
   # На Mac/Linux
   sudo dscacheutil -flushcache
   sudo killall -HUP mDNSResponder
   
   # Или просто подождите - DNS обновится автоматически
   ```

### Vercel показывает ошибку "Invalid Configuration"

1. Проверьте, что DNS-записи добавлены правильно
2. Убедитесь, что прошло достаточно времени (1-2 часа)
3. Попробуйте удалить домен в Vercel и добавить заново

### SSL-сертификат не выдается

1. Подождите 5-10 минут после обновления DNS
2. Проверьте, что домен имеет статус "Valid" в Vercel
3. Если через час не работает, обратитесь в поддержку Vercel

---

## 📞 Полезные ссылки

- [Vercel Domains Documentation](https://vercel.com/docs/concepts/projects/domains)
- [DNS Checker](https://dnschecker.org)
- [REG.RU DNS Management](https://www.reg.ru/support/help/dns-servery-i-dns-zony)

---

**Готово!** После настройки DNS ваш сайт будет доступен на `https://mysels.ru` 🚀
