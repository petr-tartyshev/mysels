# 🔗 Подключение к GitHub - Инструкция

## Что нужно сделать от вас:

### Шаг 1: Создать репозиторий на GitHub

1. Зайдите на https://github.com
2. Нажмите кнопку **"+"** в правом верхнем углу → **"New repository"**
3. Заполните:
   - **Repository name:** `sels` (или любое другое имя)
   - **Description:** "SELS - Sports Community Platform"
   - **Visibility:** Private (рекомендую) или Public
   - **НЕ** ставьте галочки на "Add a README file", "Add .gitignore", "Choose a license" (у нас уже есть файлы)
4. Нажмите **"Create repository"**

### Шаг 2: Подключить локальный проект к GitHub

После создания репозитория GitHub покажет инструкции. Выполните в терминале:

```bash
cd /Users/petr/sels

# Если Git еще не инициализирован (я это сделаю автоматически)
git init

# Добавить все файлы
git add .

# Первый коммит
git commit -m "Initial commit: SELS project setup"

# Подключить к GitHub (замените YOUR_USERNAME на ваш GitHub username)
git remote add origin https://github.com/YOUR_USERNAME/sels.git

# Переименовать ветку в main (если нужно)
git branch -M main

# Отправить код на GitHub
git push -u origin main
```

### Шаг 3: Дать мне разрешение на коммиты

После того, как репозиторий создан и подключен, я смогу:
- ✅ Автоматически коммитить изменения
- ✅ Создавать понятные сообщения коммитов
- ✅ Работать с Git напрямую

**Важно:** Мне нужно разрешение `git_write` для работы с Git. Когда вы дадите это разрешение, я смогу автоматически коммитить изменения.

---

## Что я буду делать автоматически:

После подключения к GitHub я буду:

1. **Коммитить изменения** после каждого значимого обновления
2. **Писать понятные сообщения** коммитов на русском языке
3. **Группировать изменения** логически (например, "Добавлен фильтр веломаршрутов")
4. **Не коммитить** временные файлы и конфиденциальные данные (они в `.gitignore`)

---

## Примеры сообщений коммитов, которые я буду использовать:

- `feat: добавлен фильтр веломаршрутов на карте`
- `fix: исправлена ошибка отображения аватаров`
- `style: обновлен дизайн карточки события`
- `refactor: оптимизирована структура компонентов`
- `docs: добавлен план развертывания`

---

## Текущий статус:

- ✅ `.gitignore` настроен (игнорирует node_modules, .env и т.д.)
- ⏳ Git репозиторий нужно инициализировать
- ⏳ Нужно создать репозиторий на GitHub
- ⏳ Нужно подключить remote
- ⏳ Нужно дать разрешение `git_write` для автоматических коммитов

---

## После выполнения шагов:

Как только вы:
1. Создадите репозиторий на GitHub
2. Подключите его к локальному проекту
3. Дадите мне разрешение на `git_write`

Я смогу автоматически коммитить все изменения, которые вношу в проект! 🚀
