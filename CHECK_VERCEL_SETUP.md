# ✅ Проверка настроек Vercel

Твоя настройка `DATABASE_URL` выглядит правильно! Но нужно проверить несколько вещей:

---

## ✅ Что уже правильно:

1. ✅ `DATABASE_URL` настроен
2. ✅ Формат правильный: `postgresql://postgres:...@db.dkikrvlaqencbgjlvqpx.supabase.co:5432/postgres`
3. ✅ Применяется ко всем окружениям

---

## 🔍 Что нужно проверить:

### 1. Проверь, что пароль актуальный

1. Зайди в **Supabase Dashboard** → проект → **Settings** → **Database**
2. Найди **Connection string** → **URI**
3. Скопируй полную строку
4. Сравни с тем, что в Vercel

**Если пароли отличаются:**
- Обнови `DATABASE_URL` в Vercel новым значением
- Нажми **"Save"**
- Передеплой проект (Deployments → три точки → Redeploy)

---

### 2. Проверь логи Vercel при регистрации

1. Vercel Dashboard → проект → **Deployments**
2. Открой последний deployment
3. Нажми **"Functions"** → найди `/api/auth/register`
4. Попробуй зарегистрироваться снова
5. Смотри логи в реальном времени

**Что искать в логах:**
- `DATABASE_URL не настроен` → переменная не подхватывается
- `Can't reach database server` → проблема с подключением
- `relation "users" does not exist` → таблицы не созданы
- `Prisma Client не инициализирован` → проблема с Prisma
- Другие ошибки → скопируй текст

---

### 3. Проверь таблицы в Supabase

1. Supabase Dashboard → **Table Editor**
2. Должны быть таблицы:
   - ✅ `users`
   - ✅ `posts`
   - ✅ `events`
   - ✅ `locations`
   - ✅ `conversations`
   - ✅ `conversation_participants`
   - ✅ `messages`
   - ✅ `notifications`
   - ✅ `bookings`
   - ✅ `event_requests`

**Если таблиц нет:**
- Выполни SQL из файла `all-migrations.sql` в Supabase SQL Editor

---

### 4. Проверь подключение к базе через SQL Editor

1. Supabase Dashboard → **SQL Editor**
2. Выполни запрос:
   ```sql
   SELECT COUNT(*) FROM users;
   ```
3. Если ошибка → таблицы не созданы
4. Если работает → подключение в порядке

---

## 🚀 Если всё правильно настроено

Если:
- ✅ `DATABASE_URL` правильный и актуальный
- ✅ Таблицы созданы в Supabase
- ✅ Подключение работает через SQL Editor

Но регистрация всё равно не работает:

1. **Проверь логи Vercel** при попытке регистрации
2. **Скопируй текст ошибки** из логов
3. **Отправь мне** - я помогу исправить

---

## 📝 Быстрая проверка

Выполни в терминале (если есть доступ к DATABASE_URL):

```bash
# Проверка подключения (если установлен psql)
psql "postgresql://postgres:b5#LaX8sQ/xNAvQ@db.dkikrvlaqencbgjlvqpx.supabase.co:5432/postgres" -c "SELECT COUNT(*) FROM users;"
```

Или через Supabase SQL Editor - проще и безопаснее.

---

**Готово!** После проверки этих пунктов регистрация должна заработать 🚀
