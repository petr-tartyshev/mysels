# Production Environment Variables для mysels.ru
# Скопируйте эти переменные в Vercel → Settings → Environment Variables
# НЕ коммитьте файл с реальными значениями в Git!

# База данных (Production)
# Получите строку подключения из Supabase/Neon/Railway
DATABASE_URL=postgresql://postgres:[PASSWORD]@[HOST]:5432/postgres

# Окружение
NODE_ENV=production
NEXT_PUBLIC_ENV=production

# Примеры строк подключения:
# Supabase: postgresql://postgres:[PASSWORD]@db.xxxxx.supabase.co:5432/postgres
# Neon: postgresql://[USER]:[PASSWORD]@[HOST]/[DATABASE]?sslmode=require
# Railway: postgresql://postgres:[PASSWORD]@[HOST]:5432/railway
