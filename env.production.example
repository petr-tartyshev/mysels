# Production Environment Variables для mysels.ru (Selectel + Docker + Supabase)
# Скопируйте этот файл в .env.production и заполните реальные значения.
# НЕ коммитьте файл .env.production с реальными данными в Git!

# База данных (Supabase)
# Строку подключения возьмите из Supabase → Settings → Database → Connection string → URI
DATABASE_URL=postgresql://postgres:[PASSWORD]@db.xxxxx.supabase.co:5432/postgres

# Окружение
NODE_ENV=production
NEXT_PUBLIC_ENV=production

# Хост и порт приложения внутри Docker
PORT=3000

# Примеры:
# Supabase: postgresql://postgres:[PASSWORD]@db.xxxxx.supabase.co:5432/postgres
