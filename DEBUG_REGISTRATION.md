# 🐛 Отладка ошибки регистрации

Если при регистрации появляется ошибка "Ошибка сервера", выполни следующие шаги:

---

## ✅ Шаг 1: Проверь DATABASE_URL в Vercel

1. Зайди в **Vercel Dashboard** → твой проект
2. Открой **Settings** → **Environment Variables**
3. Проверь, что есть переменная `DATABASE_URL`
4. Значение должно быть в формате:
   ```
   postgresql://postgres:[ПАРОЛЬ]@db.dkikrvlaqencbgjlvqpx.supabase.co:5432/postgres
   ```

**Если переменной нет:**
- Нажми **"Add New"**
- Name: `DATABASE_URL`
- Value: скопируй из Supabase (Settings → Database → Connection string → URI)
- Environment: выбери **Production** (и другие, если нужно)
- Нажми **"Save"**
- **Передеплой проект** (Deployments → три точки → Redeploy)

---

## ✅ Шаг 2: Проверь логи Vercel

1. Vercel Dashboard → проект → **Deployments**
2. Открой последний deployment
3. Нажми **"Functions"** → найди `/api/auth/register`
4. Попробуй зарегистрироваться снова
5. Проверь логи - там должна быть детальная ошибка

**Что искать в логах:**
- `DATABASE_URL не настроен` → нужно добавить переменную
- `Can't reach database server` → проблема с подключением к Supabase
- `relation "users" does not exist` → таблицы не созданы (нужно применить миграции)
- `Prisma Client не инициализирован` → проблема с Prisma

---

## ✅ Шаг 3: Проверь подключение к Supabase

### Вариант 1: Через Supabase Dashboard

1. Supabase Dashboard → проект → **Settings** → **Database**
2. Проверь, что база данных активна
3. Проверь **Connection string** → **URI**

### Вариант 2: Через SQL Editor

1. Supabase Dashboard → **SQL Editor**
2. Выполни простой запрос:
   ```sql
   SELECT COUNT(*) FROM users;
   ```
3. Если ошибка → таблицы не созданы (примени миграции)
4. Если работает → подключение в порядке

---

## ✅ Шаг 4: Примени миграции (если нужно)

Если в логах видишь `relation "users" does not exist`:

1. Открой файл `all-migrations.sql` в проекте
2. Скопируй всё содержимое
3. Supabase Dashboard → **SQL Editor** → **New query**
4. Вставь SQL и выполни (Run)

---

## ✅ Шаг 5: Проверь Prisma Client

Если в логах видишь ошибки Prisma:

1. В терминале выполни:
   ```bash
   cd /Users/petr/sels
   npx prisma generate
   ```

2. Закоммить и запушь изменения:
   ```bash
   git add .
   git commit -m "fix: улучшена обработка ошибок регистрации"
   git push origin main
   ```

3. Vercel автоматически пересоберёт проект

---

## 🔍 Частые ошибки и решения

### Ошибка: "DATABASE_URL не настроен"

**Решение:**
- Добавь `DATABASE_URL` в Vercel Environment Variables
- Передеплой проект

### Ошибка: "Can't reach database server"

**Решение:**
- Проверь, что Supabase проект активен
- Проверь Connection string в Supabase
- Убедись, что пароль правильный

### Ошибка: "relation 'users' does not exist"

**Решение:**
- Примени миграции через Supabase SQL Editor (файл `all-migrations.sql`)

### Ошибка: "Prisma Client не инициализирован"

**Решение:**
- Выполни `npx prisma generate` локально
- Закоммить и запушь изменения
- Vercel пересоберёт проект

---

## 📝 После исправления

1. Попробуй зарегистрировать пользователя снова
2. Если ошибка сохраняется → проверь логи Vercel
3. Скопируй текст ошибки из логов и отправь мне

---

**Готово!** После выполнения этих шагов регистрация должна заработать 🚀
