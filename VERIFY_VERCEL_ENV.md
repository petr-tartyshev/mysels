# ✅ Проверка переменных окружения в Vercel

Проверка настроек для работы регистрации пользователей.

---

## 📋 Текущие настройки (из скриншотов)

### ✅ Правильно настроено:

1. **DATABASE_URL**
   - Значение: `postgresql://postgres:b5#LaX8sQ/xNAvQ@db.dkikrvlaqencbgjlvqpx.supabase.co:5432/postgres`
   - Окружения: **All Environments** ✅
   - **Статус:** ✅ Правильно

2. **NODE_ENV**
   - Значение: `production`
   - Окружения: **All Environments** ✅
   - **Статус:** ✅ Правильно

3. **NEXT_PUBLIC_ENV**
   - Значение: `production`
   - Окружения: **All Environments** ✅
   - **Статус:** ✅ Правильно

---

## ✅ Все необходимые переменные есть

Все три обязательные переменные настроены правильно:
- ✅ `DATABASE_URL` - подключение к Supabase
- ✅ `NODE_ENV` - окружение
- ✅ `NEXT_PUBLIC_ENV` - публичное окружение

---

## 🔍 Что проверить дальше

### 1. Применены ли миграции к Supabase?

**Проверь:**
```bash
# Установи DATABASE_URL (из Vercel)
export DATABASE_URL="postgresql://postgres:b5#LaX8sQ/xNAvQ@db.dkikrvlaqencbgjlvqpx.supabase.co:5432/postgres"

# Проверь статус миграций
npx prisma migrate status
```

**Если миграции не применены:**
```bash
# Примени миграции
npx prisma migrate deploy
```

**Должно вывести:**
```
✅ All migrations have been successfully applied.
```

---

### 2. Проверь таблицы в Supabase

1. Зайди в **Supabase Dashboard** → твой проект
2. Открой **Table Editor**
3. Должны быть таблицы:
   - ✅ `users` - для пользователей
   - ✅ `posts` - для постов
   - ✅ `events` - для событий
   - ✅ `locations` - для локаций
   - ✅ `conversations` - для бесед
   - ✅ `messages` - для сообщений
   - ✅ `notifications` - для уведомлений

**Если таблиц нет:**
- Примени миграции (см. шаг 1)

---

### 3. Проверь подключение к БД

**Тест через Prisma Studio:**
```bash
export DATABASE_URL="postgresql://postgres:b5#LaX8sQ/xNAvQ@db.dkikrvlaqencbgjlvqpx.supabase.co:5432/postgres"
npx prisma studio
```

Должен открыться Prisma Studio с таблицами.

---

## 🐛 Если регистрация всё равно не работает

### Проверь логи Vercel

1. Vercel Dashboard → проект → **Deployments**
2. Выбери последний деплой
3. Открой **Functions Logs**
4. Ищи ошибки типа:
   - `Can't reach database server`
   - `Connection refused`
   - `Prisma Client not initialized`
   - `Table 'users' does not exist`

### Проверь консоль браузера

1. Открой `https://www.mysels.ru/register`
2. Нажми `F12` (открыть DevTools)
3. Перейди во вкладку **Console**
4. Попробуй зарегистрироваться
5. Ищи ошибки (красным цветом)

---

## ✅ Итоговая проверка

- ✅ `DATABASE_URL` настроен правильно
- ✅ `NODE_ENV` = `production`
- ✅ `NEXT_PUBLIC_ENV` = `production`
- ✅ Все переменные для всех окружений

**Следующий шаг:**
1. Примени миграции (если еще не применены)
2. Попробуй зарегистрировать пользователя
3. Если ошибка - проверь логи Vercel и консоль браузера

---

**Готово!** Переменные окружения настроены правильно. Если регистрация не работает, проблема скорее всего в миграциях или подключении к БД 🚀
