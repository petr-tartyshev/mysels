# 🚀 Запуск проекта на сервере Selectel

## Быстрый способ

### Шаг 1: Скопируй скрипт на сервер

На Mac:

```bash
scp setup-server.sh root@45.130.8.3:/root/
```

### Шаг 2: Зайди на сервер и выполни скрипт

```bash
ssh root@45.130.8.3
chmod +x /root/setup-server.sh
/root/setup-server.sh
```

Скрипт автоматически:
- Склонирует проект (если его нет)
- Создаст `.env.production` (если его нет)
- Соберёт Docker образ
- Запустит контейнеры

**⚠️ ВАЖНО:** Если скрипт попросит отредактировать `.env.production`:
1. Выполни: `nano /root/sels/.env.production`
2. Найди строку `DATABASE_URL=...`
3. Замени на реальный `DATABASE_URL` из Supabase
4. Сохрани: `Ctrl+O`, Enter, `Ctrl+X`
5. Запусти скрипт снова: `/root/setup-server.sh`

---

## Ручной способ (если скрипт не работает)

### 1. Зайди на сервер

```bash
ssh root@45.130.8.3
```

### 2. Склонируй проект

```bash
cd /root
git clone https://github.com/petr-tartyshev/mysels.git sels
cd sels
```

### 3. Создай `.env.production`

```bash
cp env.production.example .env.production
nano .env.production
```

В `nano`:
- Найди `DATABASE_URL=...`
- Замени на реальный из Supabase
- Сохрани: `Ctrl+O`, Enter, `Ctrl+X`

### 4. Запусти Docker

```bash
docker compose build
docker compose up -d
docker ps
```

Должен быть контейнер `sels_web` со статусом `Up`.

### 5. Проверь сайт

```bash
curl http://localhost:3000
```

Должен вернуться HTML.

---

## После запуска

Если всё работает:
- Контейнер `sels_web` запущен
- Сайт отвечает на `http://localhost:3000`

Следующий шаг: настроить Nginx + домен `mysels.ru` (см. `SELECTEL_DOCKER_DEPLOY.md`)

---

**Выполни команды и напиши, что получилось!** 🚀
