# 🚀 Применение миграций ПРЯМО СЕЙЧАС

Таблицы не созданы в Supabase. Нужно применить миграции.

---

## ⚡ Быстрый способ (через терминал)

### Шаг 1: Получи DATABASE_URL

**Из Vercel:**
1. Vercel Dashboard → проект → **Settings** → **Environment Variables**
2. Найди `DATABASE_URL`
3. Нажми на иконку **глаза** (👁️), чтобы увидеть полное значение
4. Скопируй всю строку (с паролем)

**Или из Supabase:**
1. Supabase Dashboard → проект → **Settings** → **Database**
2. **Connection string** → **URI**
3. Скопируй строку

### Шаг 2: Выполни команды

Открой терминал и выполни:

```bash
# Перейди в проект
cd /Users/petr/sels

# Установи DATABASE_URL (вставь свою строку из шага 1)
export DATABASE_URL="postgresql://postgres:[ПАРОЛЬ]@db.dkikrvlaqencbgjlvqpx.supabase.co:5432/postgres"

# Примени миграции
npx prisma migrate deploy
```

**Должно вывести:**
```
✅ All migrations have been successfully applied.
```

### Шаг 3: Проверь результат

1. Обнови страницу в Supabase Table Editor
2. Должны появиться таблицы:
   - `users`
   - `posts`
   - `events`
   - `locations`
   - `conversations`
   - `conversation_participants`
   - `messages`
   - `notifications`
   - `bookings`
   - `event_requests`

---

## 🔄 Альтернатива: через Supabase SQL Editor

Если терминал не работает, можно применить миграции через SQL Editor:

### Шаг 1: Открой SQL Editor

1. Supabase Dashboard → проект → **SQL Editor**
2. Нажми **"New query"**

### Шаг 2: Скопируй и выполни миграции

Открой файл `prisma/migrations/20260121212220_init/migration.sql` и скопируй всё содержимое в SQL Editor, затем выполни.

**Повтори для всех миграций по порядку:**
1. `20260121212220_init/migration.sql` (основные таблицы)
2. `20260122163516_add_images_to_events/migration.sql`
3. `20260122164521_make_description_optional/migration.sql`
4. `20260122170718_add_event_requests/migration.sql`
5. `20260123135819_add_level_to_events/migration.sql`

---

## ✅ После применения

После применения миграций:

1. **Проверь таблицы** в Supabase Table Editor
2. **Попробуй зарегистрировать пользователя** на `https://www.mysels.ru/register`
3. **Проверь, что данные сохраняются** в таблице `users`

---

**Готово!** После применения миграций всё заработает 🚀
