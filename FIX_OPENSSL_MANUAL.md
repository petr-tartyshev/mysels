# 🔧 Ручная установка OpenSSL в контейнер

## Проблема

OpenSSL не установлен в контейнере, поэтому Prisma не работает.

## Решение 1: Установить OpenSSL в запущенный контейнер

На сервере:

```bash
docker exec sels_web sh -c 'apk add --no-cache openssl1.1-compat'
docker restart sels_web
docker logs sels_web --tail 20
```

**⚠️ Это временное решение** - при пересборке контейнера OpenSSL исчезнет.

---

## Решение 2: Пересобрать контейнер с правильным Dockerfile

Убедись, что в Dockerfile на сервере есть строка:

```dockerfile
RUN apk add --no-cache openssl1.1-compat
```

Проверь:

```bash
cd /root/sels
grep openssl Dockerfile
```

Если строки нет - скопируй исправленный Dockerfile:

```bash
# На Mac
scp Dockerfile root@45.130.8.3:/root/sels/Dockerfile

# На сервере
cd /root/sels
docker compose build --no-cache
docker compose up -d
```

---

**Выполни Решение 1 для быстрого исправления, затем Решение 2 для постоянного исправления!**
