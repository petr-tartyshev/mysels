# Быстрая настройка SSH для GitHub

## Шаг 1: Настрой SSH ключ

```bash
./setup-github-ssh.sh
```

Скрипт покажет твой публичный ключ. **Скопируй его**.

## Шаг 2: Добавь ключ в GitHub

1. Открой: https://github.com/settings/keys
2. Нажми **"New SSH key"**
3. Вставь скопированный ключ
4. Нажми **"Add SSH key"**

## Шаг 3: Проверь подключение

```bash
ssh -T git@github.com
```

Должно появиться: `Hi petr-tartyshev! You've successfully authenticated...`

## Готово! 🎉

Теперь `git push` будет работать без пароля:

```bash
git push origin main
```

Или используй скрипт деплоя:

```bash
./deploy.sh
```

---

## Если SSH не работает

Используй Personal Access Token:

1. Создай токен: https://github.com/settings/tokens
   - Нажми "Generate new token (classic)"
   - Выбери `repo` (полный доступ)
   - Скопируй токен

2. Настрой git:
   ```bash
   git config --global credential.helper osxkeychain
   ```

3. При первом `git push`:
   - Username: `petr-tartyshev`
   - Password: `[вставь токен]`

Git сохранит токен, и больше не будет спрашивать пароль.
