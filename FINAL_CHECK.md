# ✅ Финальная проверка GitHub Actions

## Что должно быть готово:

### 1. ✅ SSH ключ создан
- Файлы `~/.ssh/github_actions` и `~/.ssh/github_actions.pub` существуют

### 2. ✅ Публичный ключ скопирован на сервер
- Ключ добавлен в `~/.ssh/authorized_keys` на сервере

### 3. ⚠️ Секреты добавлены в GitHub
Проверь в браузере:
- https://github.com/petr-tartyshev/mysels/settings/secrets/actions
- Должны быть 3 секрета: `SERVER_HOST`, `SERVER_USER`, `SERVER_SSH_KEY`

### 4. ✅ Workflow файл создан
- Файл `.github/workflows/deploy.yml` существует

---

## 🧪 Тест автоматического деплоя

### Вариант 1: Через GitHub (рекомендуется)

1. Открой: https://github.com/petr-tartyshev/mysels
2. Нажми на вкладку **"Actions"** (вверху)
3. Должен быть workflow **"Deploy to Selectel"**
4. Если его нет — сделай небольшое изменение и запушь:
   ```bash
   cd /Users/petr/sels
   echo "test" >> test.txt
   git add test.txt
   git commit -m "test: проверка деплоя"
   git push origin main
   ```
5. После push:
   - Открой вкладку **"Actions"**
   - Должен появиться запуск workflow
   - Нажми на него и смотри логи

### Вариант 2: Проверь вручную на сервере

1. Зайди на сервер:
   ```bash
   ssh root@45.130.8.3
   ```

2. Проверь, что проект есть:
   ```bash
   cd /root/sels
   ls
   ```

3. Проверь контейнеры:
   ```bash
   docker ps
   ```
   Должен быть контейнер `sels_web` со статусом `Up`

---

## ✅ Если всё работает:

- При каждом `git push` проект автоматически обновится на сервере
- GitHub Actions выполнит:
  - `git pull` на сервере
  - `docker compose build`
  - `docker compose up -d`
  - `npx prisma generate`

---

## 🐛 Если что-то не работает:

### Ошибка в GitHub Actions: "Permission denied"
- Проверь, что секрет `SERVER_SSH_KEY` содержит правильный приватный ключ
- Проверь, что публичный ключ скопирован на сервер

### Ошибка: "docker compose: command not found"
- На сервере выполни: `apt install -y docker-compose-plugin`

### Ошибка: "git pull: Permission denied"
- Проверь, что на сервере есть доступ к GitHub (репозиторий публичный)

---

**Проверь Actions в GitHub и напиши, что получилось!** 🚀
