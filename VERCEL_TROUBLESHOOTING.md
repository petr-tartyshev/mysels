# 🔧 Решение проблем с деплоем на Vercel

## Частые ошибки и их решения

### 1. Ошибка: "Command 'npm run build' exited with 1"

**Причины:**
- Ошибки TypeScript
- Отсутствуют переменные окружения
- Проблемы с Prisma

**Решение:**
1. Проверьте логи сборки в Vercel → Deployments → Build Logs
2. Убедитесь, что все переменные окружения установлены:
   - `DATABASE_URL`
   - `NODE_ENV=production`
   - `NEXT_PUBLIC_ENV=production`
3. Проверьте, что `DATABASE_URL` правильный (скопирован из Supabase/Neon)

---

### 2. Ошибка: "Route couldn't be rendered statically"

**Причина:** API route использует динамические данные (cookies, headers) без `export const dynamic = 'force-dynamic'`

**Решение:**
Добавьте в начало файла API route:
```typescript
export const dynamic = 'force-dynamic'
```

**Уже исправлено для:**
- ✅ `/api/auth/me`
- ✅ `/api/auth/login`
- ✅ `/api/auth/logout`
- ✅ `/api/auth/register`

---

### 3. Ошибка: "useSearchParams() should be wrapped in a suspense boundary"

**Причина:** `useSearchParams()` используется без Suspense

**Решение:**
Оберните компонент в Suspense:
```typescript
import { Suspense } from 'react'

function PageContent() {
  const searchParams = useSearchParams()
  // ... ваш код
}

export default function Page() {
  return (
    <Suspense fallback={<div>Загрузка...</div>}>
      <PageContent />
    </Suspense>
  )
}
```

**Уже исправлено для:**
- ✅ `/map`
- ✅ `/profile`

---

### 4. Ошибка подключения к базе данных

**Причины:**
- Неправильный `DATABASE_URL`
- База данных не запущена
- Проблемы с SSL

**Решение:**
1. Проверьте `DATABASE_URL` в Vercel → Settings → Environment Variables
2. Убедитесь, что база данных запущена (Supabase/Neon Dashboard)
3. Для Supabase добавьте `?sslmode=require` в конец строки подключения
4. Для Neon SSL включен по умолчанию

**Формат DATABASE_URL:**
```
# Supabase
postgresql://postgres:[PASSWORD]@db.xxxxx.supabase.co:5432/postgres?sslmode=require

# Neon
postgresql://[USER]:[PASSWORD]@[HOST]/[DATABASE]?sslmode=require
```

---

### 5. Ошибка: "Prisma Client not generated"

**Причина:** Prisma Client не сгенерирован во время сборки

**Решение:**
1. Убедитесь, что в `package.json` есть:
   ```json
   {
     "scripts": {
       "postinstall": "prisma generate"
     }
   }
   ```
2. Это автоматически выполнится при установке зависимостей

---

### 6. Ошибка: "Module not found" или "Cannot find module"

**Причины:**
- Отсутствуют зависимости
- Неправильный путь импорта

**Решение:**
1. Проверьте, что все зависимости в `package.json`
2. Убедитесь, что `node_modules` не в `.gitignore` (но он должен быть)
3. Vercel автоматически установит зависимости из `package.json`

---

## 📋 Чеклист перед деплоем

- [ ] Все переменные окружения установлены в Vercel
- [ ] `DATABASE_URL` правильный и доступен
- [ ] Локальная сборка проходит успешно: `npm run build`
- [ ] Все TypeScript ошибки исправлены
- [ ] Prisma миграции применены к production БД
- [ ] API routes с cookies имеют `export const dynamic = 'force-dynamic'`
- [ ] Компоненты с `useSearchParams()` обернуты в Suspense

---

## 🔍 Как проверить логи сборки в Vercel

1. Зайдите в Vercel Dashboard
2. Выберите проект
3. Откройте вкладку **"Deployments"**
4. Найдите последний деплой (с ошибкой)
5. Нажмите на него
6. Откройте **"Build Logs"**
7. Найдите строку с ошибкой (обычно красным цветом)
8. Скопируйте текст ошибки

---

## 🆘 Если ничего не помогает

1. **Очистите кэш Vercel (ВАЖНО при повторяющихся ошибках):**
   - Зайдите в Vercel Dashboard → ваш проект
   - Settings → General
   - Прокрутите вниз до "Build Cache"
   - Нажмите "Clear Build Cache"
   - Или создайте новый деплой (Deployments → три точки → Redeploy)
   - **Примечание:** Vercel может кэшировать старые версии файлов, поэтому очистка кэша часто решает проблемы

2. **Проверьте версии зависимостей:**
   - Убедитесь, что версии совместимы
   - Попробуйте обновить до последних версий

3. **Проверьте локальную сборку:**
   ```bash
   npm run build
   ```
   Если локально не работает, исправьте ошибки сначала

4. **Проверьте переменные окружения:**
   - Убедитесь, что они установлены для правильного окружения
   - Production, Preview, Development - все должны иметь `DATABASE_URL`

---

## 📞 Полезные ссылки

- [Vercel Documentation](https://vercel.com/docs)
- [Next.js Deployment](https://nextjs.org/docs/deployment)
- [Prisma Deployment](https://www.prisma.io/docs/guides/deployment)
- [Supabase Connection Strings](https://supabase.com/docs/guides/database/connecting-to-postgres)

---

**Если проблема не решена, пришлите:**
1. Точный текст ошибки из Build Logs
2. Скриншот настроек Environment Variables (без значений)
3. Результат локальной сборки: `npm run build`
